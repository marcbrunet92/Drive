dir(nom_d_un_module) -> toutes les possibilités de module.xxx
pow(x, 2) -> x²
hypot(x, y) -> la longueur de l'hypothénuse dans le cas ou les petits côtés sont x et y 
random.seed(x:int|None) -> si x vide met la seed à la date actuelle ( secondes ), sinon à l'entier, avec une meme seed, les nombres generes ensuite seront egaux ( decimale près car clacul plus ou mins précis
random.choice(x:list) -> retourne un element de la liste
random.sample(x:list, n:int) -> retourne une liste de n elements pris dans x n<=len(x)
platform.platform(aliased = False, terse=False) terse = smaller output (less precise) -> OS name
platform.machine() -> generic processor name
platform.processor() -> precise processor name
platform.system() -> generic os name
platform.version() -> OS version 
platform.python_implementation -> donne CPython ( normalement )
platform.python_version_tuple -> (major, minor, patch), the three composant of a version name
__name__ = '__main__' when run it as a script
	 = nom_module when in a module
sys.path -> list of all the location where python look for packages. Use append() to add one. use double backslaches \\
pip install --user nom_package : installe un package pour l'utilisateur local
pip install -U nom_package : upgrade le package
The Cheese Shop : reference to PyPI
a namespace is a space in which a name exist
pip --version == pip version == pip -version


The word internationalization is commonly shortened to I18N.
A classic form of ASCII code uses eight bits for each sign.
A code point is a number which makes a character. 
UCS-4 uses 32 bits (four bytes) to store each character, and the code is just the Unicode code points' unique number
UTF-8:
	ascii -> 8 bit
	non latin -> 16 bit
	idéogrammes -> 24 bits
BOM (Byte Order Mark) is a special combination of bits announcing the encoding used by a file's contents (e.g. UCS-4 or UTF-8).
Python string are immutable sequences : 'str' object does not support item assignment
in mulit lines string, the \n caracter count in the len()
chr() -> The function takes a code point and returns its character.
ord() -> take one caracter and return its code point

sequence[start : stop : step] -> stop excluded

"a".index("a") return the first occurence of the specified caracter : si le caractère n'est pas present : ValueError
"aa".count("a")
str.capitalize() -> transforme la premiere lettre en majuscule et met le reste en minuscule retourne le resultat, str non changé
str.center(a, b:str) -> retourne une chaine e longueur a avec de part et d'autre de la chaine horizontale le bon nombre de b. si b trop petit, cela retourne la chaine originale
str.endswith()
str.find() -> only for str, safer than index, return -1 si non trouvé
str.isalnum() -> True si seulement des caractères alphanumérique
str.isalpha()
str.isdigit()
str.islower()
str.isspace()
str.isupper()
",".join(["omicron", "pi", "rho"]) -> "omicron,pi,rho"
str.lower()
"www.cisco.com".lstrip("w.") -> cisco.com
str.replace(a, b, c) -> replace a by b max c times 
str.rfind() -> search from the end of the string
str.rstrip() -> strip from the end of the string
str.split() -> sans les espaces au début et a la find
str.swapcase() -> minuscule devienne majuscule
str.title() -> la premiere lettre de chaque mot devient majuscule, le reste minuscule
str. upper() 

1. Some of the methods offered by strings are:

capitalize() – changes all string letters to capitals;
center() – centers the string inside the field of a known length;
count() – counts the occurrences of a given character;
join() – joins all items of a tuple/list into one string;
lower() – converts all the string's letters into lower-case letters;
lstrip() – removes the white characters from the beginning of the string;
replace() – replaces a given substring with another;
rfind() – finds a substring starting from the end of the string;
rstrip() – removes the trailing white spaces from the end of the string;
split() – splits the string into a substring using a given delimiter;
strip() – removes the leading and trailing white spaces;
swapcase() – swaps the letters' cases (lower to upper and vice versa)
title() – makes the first letter in each word upper-case;
upper() – converts all the string's letter into upper-case letters.
text.find(ch, start_position)

2. String content can be determined using the following methods (all of them return Boolean values):

endswith() – does the string end with a given substring?
isalnum() – does the string consist only of letters and digits?
isalpha() – does the string consist only of letters?
islower() – does the string consists only of lower-case letters?
isspace() – does the string consists only of white spaces?
isupper() – does the string consists only of upper-case letters?
startswith() – does the string begin with a given substring?

3. String comparaison

'alpha' < 'alphabet'
'beta' > 'Beta' -> minuscule supérieure au majuscule
'alpha' == 'alpha'
'alpha' != 'Alpha'
'10' != '010' car le premier caractère 1 est superieur a 0
str == number -> False
majuscule inferieure au minuscule

Pour inverser une liste :  text[::-1]